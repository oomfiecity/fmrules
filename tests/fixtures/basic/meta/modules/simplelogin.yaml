module: simplelogin
description: "Expand from: through the SimpleLogin X-SimpleLogin-Original-From header"
targets: from
transform:
  from:
    any:
      - from: "{value}"
      - header:
          name: X-SimpleLogin-Original-From
          value: "{value}"
