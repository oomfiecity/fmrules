rules:
  - name: any-into-all
    enabled: true
    continue: true
    when:
      all:
        - extends: [snippets/any-snippet.yml]
        - subject: { contains: urgent }
    actions:
      - add_label: "Test"
