rules:
  - name: all-into-all
    enabled: true
    continue: true
    when:
      all:
        - extends: [snippets/all-snippet.yml]
        - subject: { contains: urgent }
    actions:
      - add_label: "Test"
